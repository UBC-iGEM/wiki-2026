image: alpine:3.20

pages:
    stage: deploy
    script:
        - test -s public/index.html
    artifacts:
        paths:
            - public
    rules:
        - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
